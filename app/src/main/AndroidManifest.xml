<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.categoryapp" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CategoryApp" >
        <activity android:name=".Controlleur.ConnexionReussieActivity" >
        </activity>
        <activity android:name=".Controlleur.AboutActivity" />
        <activity android:name=".Controlleur.ConnexionActivity" />
        <activity android:name=".Controlleur.InscriptionActivity" />
        <activity android:name=".Controlleur.CategoryActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>